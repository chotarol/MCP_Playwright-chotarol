# Saucedemo Manual Test Report

- **Scenario:** Verify core e-commerce flows on https://www.saucedemo.com (login, browse, add to cart, checkout, logout).

- **Steps Taken:**
  - Opened the site with the Playwright MCP browser.
  - Attempted login using demo credentials (`standard_user` / `secret_sauce`) via multiple interaction methods (direct `.value` set, dispatched `input` events, synthetic clicks).
  - Attempted to navigate directly to `/inventory.html` to continue flows when login did not complete.
  - Captured screenshots of the current page state.

- **Outcome:**
  - The site loaded successfully (login page visible).
  - All automated login attempts did not result in a successful navigation to the inventory page; the page remained on the login screen.
  - Because login could not be completed in this environment, downstream flows (add-to-cart, cart review, checkout, logout) could not be exercised end-to-end by this automated agent.

- **Evidence / Artifacts:**
  - Screenshot: ![Login page screenshot](manual-tests/screenshots/current.png)
  - Page snapshot and console logs saved in the Playwright MCP session (internal snapshots).

- **Issues Found / Observations:**
  - Inputs were programmatically set but the application did not transition to the authenticated `/inventory.html` view. The app appears to ignore programmatic input or blocks automated logins in this execution environment.
  - Console output during the session showed JavaScript errors (saved in MCP logs). These may be related to automation or normal app behavior under test.

- **Impact:**
  - Automated end-to-end verification of checkout and cart flows couldn't be completed from this MCP environment.

- **Recommendations / Next Steps:**
  - Run a real Playwright run locally in headed mode (or on a CI runner that allows full browser automation) to avoid any MCP-specific restrictions. Example quick test run:

```bash
npm install
npx playwright test --project=chromium --headed
```

  - If you want, I can generate a Playwright test file (`tests/saucedemo.spec.js`) that performs: login, add item to cart, verify cart, checkout, and logout. You can run it locally to confirm.


---

_Report generated by an automated test agent (manual/assisted exploration)._
